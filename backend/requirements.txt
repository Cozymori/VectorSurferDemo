# VectorSurfer 2.0 Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# VectorWave SDK (local)
vectorwave

# VectorWave dependencies (should already be installed)
weaviate-client>=4.0.0
python-dotenv>=1.0.0

# Optional: For OpenAI RAG features
openai>=1.0.0

# Optional: For HuggingFace vectorizer
sentence-transformers>=2.2.0

# [추가] For async batch processing in sync context
nest-asyncio>=1.5.0
